# compute the max of 3 numbers

decl a number 1
decl b number 2
decl c number 3

decl max number a

if > b max
    set max b
end

if > c max
    set max c
end

# or: 
# set max ? > b max b max
# set max ? > c max c max

-> max
